package com.opensource.qa;

public class InfoSelenium {
	//Selenium son librerias que se adaptan al lenguaje de codigo que se utiliza para interactuar con una plataforma web
	//Simular interaccion humana con una plataforma
	
	/*
	 * Versiones de Selenium
	 * 1. Selenium IDE: Reord & Play
	 * 2. Selenium RC o Remote Control
	 * 3. Selenium WebDriver: Manejador de una plataforma web (Chrome, Firewox, Internet Explorer, Edge, Opera) 
	 * 4. Selenium Grid: CI (Continuous Integration)
	 * 
	 */
	
	/*
	 * Web Elements u objetos 
	 * Locator o propiedades
	 * 1. ID
	 * 2. Name
	 * 3. Link Text
	 * 4. Partial Link Text
	 * 5. Class
	 * 6. Tag
	 * 7. ***CSS
	 * 8. ***Xpath***
	 */
	
	/*XPATH: 
	 * Se define como la ruta del XML o XML Path (DOM)
	 * Se define como una sintaxis para encontrar un elemento.
	 * Existen dos tipos de XPATH (XPATH Relativo y XPATH Absoluto)
	 */

	/*
	 * PAUSAS Y SYNC
	 * Tipos de Wait
	 * 1. Implicito (Implicit wait) se usa cuando se valida el DOM
	 * 2. Explicito se usa cuando se valida un objeto en especifico (Nombre del tab, nombre del URL, hasta que un objeto sea visible o invisible) Valida
	 * condiciones
	 * 
	 * 	 
	 *   
	 *   
	 */
}
